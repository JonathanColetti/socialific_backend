interface scores {
    id: number,
    sid: number,
    score: number,
}

function getTop(object: any) {
    // Get score array sorted from top to bottom from db
    let arraytop: Array<scores> = []
    
}